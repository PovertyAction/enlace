# Director for data files

Do not commit data to GitHub. Instead, use this directory to document the data files used in the project.
All data files should be gitignored via the `.gitignore` file in the project root.
